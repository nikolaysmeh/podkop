# ============================================================
# TARGET — demo destination for forwarded webhooks
# ============================================================
# Copy to target/.env and customize.
#
# TARGET_PORT is NOT set here — it belongs in the root .env
# (copy from .env.sample) because Docker Compose uses it for
# host port mapping in addition to passing it into the container.
# ============================================================
